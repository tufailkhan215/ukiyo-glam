{% comment %} Grid banner with product – banners + product blocks in same grid {% endcomment %}
<section class="ug-grid-banner ug-grid-banner--with-product section-{{ section.id }}-padding" aria-label="{{ section.settings.heading | default: 'Promo grid' }}">
  <div class="ug-grid-banner__container page-width">
    {% if section.settings.heading != blank or section.settings.subheading != blank %}
      <div class="ug-grid-banner__heading-wrap ug-grid-banner__heading-wrap--{{ section.settings.heading_alignment }}">
        {% if section.settings.subheading != blank %}<p class="ug-grid-banner__subheading">{{ section.settings.subheading }}</p>{% endif %}
        {% if section.settings.heading != blank %}<h2 class="ug-grid-banner__heading">{{ section.settings.heading }}</h2>{% endif %}
      </div>
    {% endif %}
    <div class="ug-grid-banner__grid-wrap">
      <div class="ug-grid-banner__grid ug-grid-banner__grid--with-product ug-grid-banner__grid--{{ section.settings.columns }}" style="--ug-grid-gap: {{ section.settings.grid_gap }}px;">
        {% for block in section.blocks %}
          {% if block.type == 'banner' %}
            <div class="ug-grid-banner__item ug-grid-banner__item--content-{{ block.settings.content_position }}" {{ block.shopify_attributes }}>
              <a href="{{ block.settings.link | default: '#' }}" class="ug-grid-banner__link">
                <span class="ug-grid-banner__media">
                  {% if block.settings.image != blank %}
                    <img src="{{ block.settings.image | image_url: width: 800 }}" alt="{{ block.settings.heading | default: block.settings.image.alt | escape }}" width="800" height="800" loading="lazy" class="ug-grid-banner__image">
                  {% else %}
                    {{ 'image' | placeholder_svg_tag: 'ug-grid-banner__placeholder' }}
                  {% endif %}
                  {% if block.settings.show_overlay %}
                    <span class="ug-grid-banner__overlay" style="--ug-overlay-opacity: {{ block.settings.overlay_opacity | default: 40 | divided_by: 100.0 }};" aria-hidden="true"></span>
                  {% endif %}
                </span>
                <span class="ug-grid-banner__content">
                  {% if block.settings.heading != blank %}<span class="ug-grid-banner__title">{{ block.settings.heading }}</span>{% endif %}
                  {% if block.settings.button_label != blank %}<span class="ug-grid-banner__btn ug-btn ug-btn--secondary">{{ block.settings.button_label }}</span>{% endif %}
                </span>
              </a>
            </div>
          {% elsif block.type == 'product' and block.settings.product != blank %}
            <div class="ug-grid-banner__item ug-grid-banner__item--product" {{ block.shopify_attributes }}>
              {% render 'ug-card-product', product: block.settings.product %}
            </div>
          {% endif %}
        {% endfor %}
      </div>
    </div>
  </div>
</section>

<style>
  .section-{{ section.id }}-padding { padding-top: {{ section.settings.padding_top }}px; padding-bottom: {{ section.settings.padding_bottom }}px; }
</style>

{% schema %}
{
  "name": "Grid banner with product",
  "tag": "section",
  "class": "ug-section-grid-banner-with-product",
  "max_blocks": 12,
  "settings": [
    { "type": "text", "id": "heading", "label": "Heading" },
    { "type": "text", "id": "subheading", "label": "Subheading" },
    { "type": "select", "id": "heading_alignment", "options": [{"value": "left", "label": "Left"}, {"value": "center", "label": "Center"}, {"value": "right", "label": "Right"}], "default": "center", "label": "Heading alignment" },
    { "type": "select", "id": "columns", "options": [{"value": "2", "label": "2"}, {"value": "3", "label": "3"}, {"value": "4", "label": "4"}], "default": "3", "label": "Columns" },
    { "type": "range", "id": "grid_gap", "min": 0, "max": 40, "step": 2, "default": 16, "unit": "px", "label": "Grid gap" },
    { "type": "range", "id": "padding_top", "min": 0, "max": 100, "step": 4, "default": 36, "unit": "px", "label": "Padding top" },
    { "type": "range", "id": "padding_bottom", "min": 0, "max": 100, "step": 4, "default": 36, "unit": "px", "label": "Padding bottom" }
  ],
  "blocks": [
    {
      "type": "banner",
      "name": "Banner",
      "settings": [
        { "type": "image_picker", "id": "image", "label": "Image" },
        { "type": "text", "id": "heading", "label": "Heading" },
        { "type": "text", "id": "button_label", "label": "Button label" },
        { "type": "url", "id": "link", "label": "Link" },
        { "type": "select", "id": "content_position", "options": [{"value": "bottom-left", "label": "Bottom left"}, {"value": "bottom-center", "label": "Bottom center"}, {"value": "center", "label": "Center"}], "default": "bottom-left", "label": "Content position" },
        { "type": "checkbox", "id": "show_overlay", "default": true, "label": "Show overlay" },
        { "type": "range", "id": "overlay_opacity", "min": 0, "max": 100, "step": 5, "default": 40, "unit": "%", "label": "Overlay opacity" }
      ]
    },
    {
      "type": "product",
      "name": "Product",
      "settings": [
        { "type": "product", "id": "product", "label": "Product" }
      ]
    }
  ],
  "presets": [{ "name": "Grid banner with product", "blocks": [{"type": "banner"}, {"type": "banner"}, {"type": "product"}] }]
}
{% endschema %}
