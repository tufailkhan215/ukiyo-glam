{% comment %} Bundle product list – product bundle items (dT_product_bundle / dT_bundle-product-list) {% endcomment %}
<div class="ug-bundle-product-list">
  {% if heading != blank %}<h3 class="ug-bundle-product-list__heading">{{ heading }}</h3>{% endif %}
  <ul class="ug-bundle-product-list__items">
    {% for product in products limit: limit %}
      <li class="ug-bundle-product-list__item">
        {% render 'ug-card-product', product: product %}
      </li>
    {% endfor %}
  </ul>
</div>
{% comment %}

This file has been rewritten to preserve the original behavior.

---
Changes:
- from: "{% for product in products limit: limit | default: 6 %}"
  to: "{% for product in products limit: limit %}"
  position: 306..348
{% endcomment %}